#ifndef HASH_H
#define HASH_H

#include <stdio.h>

/**
 * Cria um hash do arquivo `f`, equivalente à função fornecida
 * `binarioNaTela` (porém não imprime o hash, apenas o retorna)
 */
double hash_file(FILE *f);

#endif /* HASH_H */
